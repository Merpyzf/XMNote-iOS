# 书摘导入对齐情况

更新日期：2026-09-07

| 功能点 | iOS 行为 | Android 行为 | 对齐状态 | 双端代码证据 | 评估 |
| --- | --- | --- | --- | --- | --- |
| 已选书导入 | 只将选中且可提交的候选提交给 Repository，写入前可编辑目标与内容。 | Presenter 过滤选中书后调用 `NoteRepository.importNotesToDbRx`。 | 已对齐（交互增强） | iOS：`xmnote/ViewModels/Personal/NoteImportPreviewViewModel.swift:176`、`:201`；Android：`app/src/main/java/com/merpyzf/xmnote/mvp/presenter/data/ImportPresenter.kt:43`、`:62`。 | iOS 增加预览确认，降低误写入风险。 |
| 目标书匹配 | 先按微信读书可靠身份匹配，再按本地书名规则给出自动、候选或无匹配状态。 | 导入前通过 `fillBookInfo` 补全目标书资料。 | 部分对齐 | iOS：`xmnote/Data/Repositories/NoteImportRepository.swift:131`；Android：`app/src/main/java/com/merpyzf/xmnote/mvp/presenter/data/ImportPresenter.kt:56`、`:103`。 | 功能优化：iOS 显式暴露候选和未解决状态，而非后台静默补全。 |
| 取消与异步回填 | 主线程会话取消未提交任务，并以版本票据避免迟到结果覆盖新选择。 | Presenter 通过 Job 取消，匹配 helper 在取消时关闭 executor。 | 已对齐（可靠性增强） | iOS：`xmnote/ViewModels/Personal/NoteImportPreviewViewModel.swift:53`、`:141`、`:205`；Android：`app/src/main/java/com/merpyzf/xmnote/mvp/presenter/data/ImportPresenter.kt:99`、`:103`。 | iOS 使用 structured concurrency 表达同一取消意图。 |
| 部分失败结果 | 按提交组保留成功、跳过和失败结果，页面不把部分写入误报为完全失败。 | Android 捕获整批异常并提示错误，已成功项需要由底层结果判断。 | 有意差异 | iOS：`xmnote/ViewModels/Personal/NoteImportPreviewViewModel.swift:69`、`:71`；Android：`app/src/main/java/com/merpyzf/xmnote/mvp/presenter/data/ImportPresenter.kt:67`。 | 功能优化；建议 Android 补充逐书结果反馈。 |
| 多来源入口 | 使用统一草稿模型承接文件、剪贴板、API 和微信读书。 | `NotesLoadPresenter` 按来源分派解析与导入。 | 已对齐 | iOS：`xmnote/Views/Personal/DataImport/UnifiedNoteImportViews.swift:1`；Android：`app/src/main/java/com/merpyzf/xmnote/mvp/presenter/data/NotesLoadPresenter.kt:369`。 | 保持业务意图，界面按 iOS 设计系统重构。 |

## 未验证项

构建和静态工具检查通过；尚未进行真实第三方授权、外部文件全量样本、数据库快照、App UI Test 或视觉验收。上述未验证项不应表述为功能已经完成验收。
