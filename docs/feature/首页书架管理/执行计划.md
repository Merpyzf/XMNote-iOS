# 首页书架管理迁移执行计划

## 文档定位

本文用于跟踪 Android 首页书架管理迁移到 iOS 的完整执行过程。迁移允许在产品体验、入口组织、视觉表达和操作反馈上做 iOS 原生优化，但所有会修改数据的操作必须严格对齐 Android 当前实现，避免 iOS 写入后在 Android 端同步、读取或继续编辑时出现语义偏差。

## 状态标记

| 标记 | 含义 | 使用规则 |
| --- | --- | --- |
| `[ ]` | 未开始 | 任务尚未进入执行 |
| `[~]` | 进行中 | 正在分析、实现或验证 |
| `[x]` | 已完成 | 任务已完成，且必要验证已记录 |
| `[!]` | 阻塞或需决策 | 发现数据语义、产品边界或技术实现阻塞 |
| `[D]` | 已完成 Android 数据语义核对 | 已核对 Android owner、DAO SQL、事务边界、时间戳与软删除规则 |
| `[V]` | 已完成 iOS 写入验证 | 已验证 iOS 写入后的数据库结果与 Android 语义一致 |

所有会改数据的任务必须先完成 `[D]`，再进入 iOS Repository 实现；实现与验证通过后再补 `[V]` 和 `[x]`。每次完成任务时，在对应任务后追加完成日期、验证方式与关键结论。

## 执行日志

| 日期 | 标记 | 事项 | 结果 |
| --- | --- | --- | --- |
| 2026-05-06 | `[x]` | 创建首页书架管理迁移执行计划 | 建立任务清单、数据语义红线与后续打标规则 |
| 2026-05-06 | `[x]` | 创建首页书架管理交互设计文档 | 固定 iOS 端 UI/UX 方案，并标识相较 Android 的优化点 |
| 2026-05-06 | `[x]` | 创建首页书架管理分阶段实施方案 | 固定纵向切片节奏、阶段验收口径与 Android 对齐闸门 |
| 2026-05-06 | `[x]` | 完成第一轮 Phase 0 + Phase 1 | 已核对 Android 默认书架只读路径，iOS 完成 Book/Group 混排读取与默认网格展示；构建通过，本轮不写库、不影响同步 |
| 2026-05-06 | `[x]` | 完成第二轮 Phase 2 只读骨架 | 已新增六个书架维度、搜索入口与显示设置 Sheet；构建通过，本轮设置仅保存在内存中，不写库、不影响同步 |

## 数据语义红线

- Android DAO SQL 是数据修改语义源；iOS 不以产品优化为理由改变最终落库结果。
- iOS ViewModel 禁止直接访问数据库；所有读写必须经过 Repository。
- 任何写操作实现前，必须先完成以下核对：
  - Android owner
  - Android DAO SQL
  - 事务边界
  - `is_deleted` 条件
  - `updated_date` 是否更新
  - `last_sync_date` 是否保持不动
  - 影响表范围
  - iOS 对应 Repository 写入点
  - iOS 写入后同步到 Android 的风险结论
- Android 未更新 `updated_date` 的操作，iOS 默认也不更新，避免制造 Android 不会产生的同步事件。
- Android 会更新 `updated_date` 的操作，iOS 必须同步更新，避免 iOS 写入无法被 Android 增量同步识别。
- 如需偏离 Android 数据规则，必须先提交双端代码证据、行为差异、风险评估和回滚方案，获得明确确认后才能落地。

## 已确认事实基线

### Android 侧

| 能力 | Android owner | 关键事实 |
| --- | --- | --- |
| 网格手动排序 | `/Users/wangke/Workspace/AndroidProjects/XMNote/app/src/main/java/com/merpyzf/xmnote/ui/main/fragment/book/DefaultBookListFragment.kt:1124` | `rememberReorderableLazyGridState` 驱动网格拖拽，结束后回调 ViewModel |
| 排序写入 | `/Users/wangke/Workspace/AndroidProjects/XMNote/data/src/main/java/com/merpyzf/data/repository/BookRepository.kt:700` | `updateBookDataListOrder` 按最终下标写 `book_order` / `group_order` |
| 书籍排序 DAO | `/Users/wangke/Workspace/AndroidProjects/XMNote/data/src/main/java/com/merpyzf/data/dao/BookDao.kt:63` | `updateBookOrderSuspend` 不更新 `updated_date`，条件包含 `is_deleted = 0` 与 `book.id != 0` |
| 分组排序 DAO | `/Users/wangke/Workspace/AndroidProjects/XMNote/data/src/main/java/com/merpyzf/data/dao/GroupDao.kt:44` | `updateGroupOrderSuspend` 不更新 `updated_date` |
| 移到最前/最后 | `/Users/wangke/Workspace/AndroidProjects/XMNote/app/src/main/java/com/merpyzf/xmnote/viewmodel/book/DefaultBookListViewModel.kt:386` | 只移动非置顶选中项，置顶项保持置顶区顺序 |
| 置顶/取消置顶 | `/Users/wangke/Workspace/AndroidProjects/XMNote/data/src/main/java/com/merpyzf/data/repository/BookRepository.kt:3406` | Book 与 Group 共用全局最大 `pin_order`，取消置顶写 `pinned = 0, pin_order = 0` |
| 置顶 DAO | `/Users/wangke/Workspace/AndroidProjects/XMNote/data/src/main/java/com/merpyzf/data/dao/BookDao.kt:782`、`/Users/wangke/Workspace/AndroidProjects/XMNote/data/src/main/java/com/merpyzf/data/dao/GroupDao.kt:130` | 置顶写入不更新 `updated_date` |
| 删除书籍 | `/Users/wangke/Workspace/AndroidProjects/XMNote/data/src/main/java/com/merpyzf/data/repository/BookRepository.kt:783` | `deleteBook` 在事务内软删除书籍及 book_id 关联数据 |
| 删除分组 | `/Users/wangke/Workspace/AndroidProjects/XMNote/data/src/main/java/com/merpyzf/data/repository/BookRepository.kt:759` | 删除 Group 前先 `moveOut` 组内书籍，再 `deleteGroup` |
| 分组移出 | `/Users/wangke/Workspace/AndroidProjects/XMNote/data/src/main/java/com/merpyzf/data/repository/GroupRepository.kt:156` | 移出时取消置顶、软删除 `group_book`、按开头/末尾写新 `book_order` |
| 移入分组 | `/Users/wangke/Workspace/AndroidProjects/XMNote/data/src/main/java/com/merpyzf/data/repository/GroupRepository.kt:71` | 事务内取消置顶、删除旧关系、插入新关系、写目标分组内 `book_order = max + 1` |
| 默认书架读取 | `/Users/wangke/Workspace/AndroidProjects/XMNote/data/src/main/java/com/merpyzf/data/repository/BookRepository.kt:1872` | `getDefaultBookList(CUSTOM)` 先取非有效分组内书籍，再追加有效分组；空分组不进入顶层列表 |
| 顶层非分组书 SQL | `/Users/wangke/Workspace/AndroidProjects/XMNote/data/src/main/java/com/merpyzf/data/dao/BookDao.kt:219` | `queryNotInGroupBookIdsSuspend` 过滤 `book.is_deleted = 0`、`book.id != 0`，并排除有效 `group_book` 关系内的书籍 |
| 默认分组读取 SQL | `/Users/wangke/Workspace/AndroidProjects/XMNote/data/src/main/java/com/merpyzf/data/dao/GroupDao.kt:82` | `queryGroupListSuspend` 只取 `is_deleted = 0` 的分组，按 `group_order ASC` 读取 |
| 默认手动排序格式化 | `/Users/wangke/Workspace/AndroidProjects/XMNote/app/src/main/java/com/merpyzf/xmnote/helper/book/BookListFormatHelper.kt:24` | `formatByCustom` 将置顶项按 `pinnedOrder DESC` 放前，普通项按 `sortOrder ASC` 放后，并触发组内排序 |
| 组内自定义排序 | `/Users/wangke/Workspace/AndroidProjects/XMNote/app/src/main/java/com/merpyzf/xmnote/helper/book/SubBookListFormatHelper.kt:176` | `reformatByCustom` 将置顶书按 `pin_order DESC` 放前，普通书按 `book_order ASC` 放后 |

### iOS 侧

| 能力 | iOS 现状 | 关键事实 |
| --- | --- | --- |
| 旧书架读取 | `xmnote/Data/Repositories/BookRepository.swift:21` | `observeBooks()` 保留，继续服务旧调用与详情相关链路 |
| 默认书架混排读取 | `xmnote/Data/Repositories/BookRepository.swift:28` | 新增 `observeBookshelf(setting:searchKeyword:)`，只读订阅 Book/Group 混排结果 |
| 默认混排排序 | `xmnote/Data/Repositories/BookRepository.swift:247` | Swift 层复刻 Android `formatByCustom`：置顶 `pin_order DESC`，普通 `book_order/group_order ASC` |
| 书架统一模型 | `xmnote/Domain/Models/BookModels.swift:22` | 新增 `BookshelfItemID`、`BookshelfItem`、Book/Group payload，避免 Book 与 Group ID 冲突 |
| Repository 契约 | `xmnote/Domain/Repositories/RepositoryProtocols.swift:13` | `BookRepositoryProtocol` 已新增只读混排书架订阅；尚无书架管理写入契约 |
| 书架维度模型 | `xmnote/Domain/Models/BookModels.swift:29` | 新增 `BookshelfDimension`、`BookshelfSnapshot`、状态分区、标签/来源/作者聚合、评分分区等只读模型 |
| 书架快照读取 | `xmnote/Data/Repositories/BookRepository.swift:38` | 新增 `observeBookshelfSnapshot(setting:searchKeyword:)`，统一输出默认混排与五类聚合维度 |
| 维度状态编排 | `xmnote/ViewModels/Book/BookViewModel.swift:25` | `BookViewModel` 新增 `selectedDimension`、`searchKeyword`、`isSearchActive`、`displaySetting`，仍只订阅 Repository |
| 维度 UI 骨架 | `xmnote/Views/Book/BookGridView.swift:19` | `BookGridView` 新增维度 rail、搜索态提示、默认/状态/标签/来源/评分/作者六类只读展示 |
| 显示设置入口 | `xmnote/Views/Book/Sheets/BookshelfDisplaySettingSheet.swift:11` | 新增显示设置 Sheet，仅修改内存态布局、列数与书摘数量显示偏好 |
| Book 字段 | `xmnote/Database/Records/BookRecord.swift:13` | 已有 `bookOrder`、`pinned`、`pinOrder`、`updatedDate`、`lastSyncDate`、`isDeleted` |
| Group 字段 | `xmnote/Database/Records/GroupRecord.swift:13` | 已有 `groupOrder`、`pinned`、`pinOrder`、`updatedDate`、`lastSyncDate`、`isDeleted` |
| GroupBook 字段 | `xmnote/Database/Records/GroupBookRecord.swift:13` | 已有 `groupId`、`bookId`、`updatedDate`、`lastSyncDate`、`isDeleted` |

## 第一轮只读对齐结论

- `[x]` 第一轮只做默认书架只读混排，不新增 migration，不新增任何写库接口。
- `[x]` iOS 不写 `book_order`、`group_order`、`pinned`、`pin_order`，不更新 `updated_date` / `last_sync_date`，因此本轮不产生 iOS 到 Android 的同步语义变化。
- `[x]` 顶层 Book 读取对齐 Android：只保留 `book.is_deleted = 0`、`book.id != 0`，并排除仍处于有效分组关系中的书籍。
- `[x]` 顶层 Group 读取对齐 Android：只保留 `group.is_deleted = 0` 且组内存在有效书籍的分组；空分组不展示。
- `[x]` 默认手动排序读取对齐 Android：置顶 Book/Group 按 `pin_order DESC`，普通 Book/Group 按 `book_order/group_order ASC`。
- `[x]` 组内代表封面按最多 6 本有效书生成，组内书籍顺序按置顶 `pin_order DESC` + 普通 `book_order ASC` 处理。
- `[x]` 构建验证：`xcodebuild -project xmnote.xcodeproj -scheme xmnote -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build` 通过。

## 第二轮只读骨架结论

- `[x]` 第二轮只做 Phase 2 只读骨架：默认、状态、标签、来源、评分、作者六个浏览维度均已接入。
- `[x]` 非默认维度基于全量有效 Book 聚合，过滤条件为 `book.is_deleted = 0` 且 `book.id != 0`，不把 Group 作为聚合对象。
- `[x]` 标签维度按有效书籍标签聚合；无标签固定归入“未设置标签”。
- `[x]` 来源维度按有效来源聚合；隐藏、缺失或无效来源归入“未知来源”。
- `[x]` 状态维度按有效阅读状态聚合；无效状态归入“未设置状态”。
- `[x]` 评分维度将 `score = 0` 归入“未评分”，其余按 `score / 2` 展示半星粒度分值。
- `[x]` 作者维度将空作者归入“未知作者”，其余通过 Foundation 拉丁化生成右侧索引分区，失败归入 `#`。
- `[x]` 搜索态只做只读过滤：默认维度过滤 Book 与 Group；聚合维度基于过滤后的 Book 重新计算可见分组。
- `[x]` 显示设置仅保存在 `BookViewModel` 内存态，不写 UserDefaults、数据库或同步字段。
- `[x]` 本轮不写 `book_order`、`group_order`、`pinned`、`pin_order`、`updated_date`、`last_sync_date`，不产生 iOS 到 Android 的同步语义变化。
- `[x]` 构建验证：`xcodebuild -project xmnote.xcodeproj -scheme xmnote -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build` 通过。

## 产品迁移原则

- 首页不机械复制 Android 的入口密度，优先用 iOS 原生编辑态、上下文菜单、底部操作区和分层设置组织能力。
- Android 首页书架的业务能力需要覆盖，但可以拆分为首页高频动作和二级管理动作。
- 手动排序在 iOS 端使用 SwiftUI 自建网格拖拽能力承接，不把 `List.onMove` 当作当前三列书架的平替。
- 搜索过滤态默认禁用手动排序，并说明排序需要回到完整书架列表执行。
- 置顶、排序、删除、分组等最终数据结果必须与 Android 完全一致。

## 执行任务清单

### 0. 数据语义基线

- `[~]` 建立 Android 写操作对照表，覆盖排序、置顶、删除、分组、批量移动、批量属性修改。（2026-05-06：已补默认书架只读路径；写操作矩阵继续推进）
- `[~]` 建立 iOS 当前 schema / record / repository 对照表，确认是否存在缺字段、缺索引、缺迁移或语义偏差。（2026-05-06：已确认第一轮只读依赖的 `book` / `group` / `group_book` 字段可用）
- `[ ]` 明确每项操作是否更新 `updated_date`，并逐项记录 Android DAO SQL 证据。
- `[ ]` 明确每项操作是否影响 iOS 到 Android 同步，标出高风险写入。
- `[~]` 形成“允许产品优化 / 禁止数据偏离”清单，作为后续评审依据。（2026-05-06：第一轮结论为只读无同步影响；后续写操作继续受 `[D]` 闸门约束）

完成标准：
- 每个写操作都有 Android owner、DAO SQL、事务边界、时间戳、软删除、iOS 对应实现点。
- 发现不确定项时标 `[!]`，不允许带着不确定项进入 Repository 写入实现。

### 1. 只读首页书架

- `[x]` 对齐 Book / Group 混排读取模型，定义 iOS 书架列表统一 item 类型。（2026-05-06：新增 `BookshelfItemID` / `BookshelfItem` / payload）
- `[x]` 对齐置顶区和普通区读取顺序：置顶优先，置顶按 `pin_order`，普通项按 `book_order` / `group_order`。（2026-05-06：Repository Swift 层按 Android `formatByCustom` 排序）
- `[x]` 补齐分组卡片读取所需字段和组内书籍摘要。（2026-05-06：有效书籍数 + 最多 6 张代表封面）
- `[x]` 保持 ViewModel 只走 Repository，不直接访问 GRDB。（2026-05-06：`BookViewModel` 订阅 `observeBookshelf`）
- `[x]` 处理空书架、只有分组、只有置顶项、分组为空、封面为空、长标题等展示场景。（2026-05-06：空态、Group 过滤、封面占位与标题截断已接入；待后续手动验收补充截图）

完成标准：
- 首页能稳定展示 Book / Group 混排列表。
- iOS 读取结果与 Android 书架排序语义一致。
- 不涉及写入，不需要 `[D]` / `[V]`。

### 2. 显示与筛选

- `[x]` 迁移网格/列表显示模式。（2026-05-06：显示设置 Sheet 已支持内存态切换，默认维度可在网格/列表间切换）
- `[x]` 迁移列数、封面大小或密度相关设置。（2026-05-06：列数支持 2 到 4 列，当前仅影响 UI，不写入持久化配置）
- `[x]` 迁移排序模式与“手动排序”模式入口。（2026-05-06：设置 Sheet 显示当前“手动排序”读取语义，本轮不启用拖拽或写库）
- `[x]` 迁移分区展示策略，明确置顶区、普通区、分组的可见规则。（2026-05-06：默认维度沿用 Phase 1 混排排序；状态/评分使用分区卡，标签/来源/作者使用聚合卡）
- `[x]` 搜索过滤态禁用手动排序，并给出轻量说明。（2026-05-06：搜索态仅过滤只读快照，页面提示搜索结果不支持排序）
- `[x]` 产品上降低 Android 当前显示设置的复杂度，但保持数据读取结果可对齐。（2026-05-06：顶部只保留搜索、设置、新增三个入口；低频设置进入 Sheet）

完成标准：
- 用户可以在 iOS 原生设置入口完成首页展示偏好调整。
- 非手动排序模式下不能触发拖拽写入。
- 搜索态不允许提交排序写入。

### 3. 编辑态与选择

- `[ ]` 建立 iOS 原生编辑态，支持进入、退出、批量选择和反选策略。
- `[ ]` 建立选中集合模型，支持 Book / Group 混选。
- `[ ]` 重组操作入口：高频操作留在编辑态底部，低频操作进入更多菜单或二级页。
- `[ ]` 所有写操作入口在执行前检查是否已完成对应 `[D]` 数据核对。
- `[ ]` 写操作执行时提供即时反馈并禁用重复提交。
- `[ ]` 写入失败时保留错误提示和 UI 回滚策略。

完成标准：
- 编辑态选择行为稳定。
- ViewModel 不直接访问数据库。
- 未完成 `[D]` 的写操作不得进入正式实现。

### 4. 排序与置顶

- `[ ] [D]` 手动排序数据规则核对。
  - Android owner：`BookRepository.updateBookDataListOrder`
  - 必核字段：`book.book_order`、`group.group_order`
  - 必核时间戳：不更新 `updated_date`
  - 必核过滤：Book DAO 包含 `is_deleted = 0` 与 `book.id != 0`
- `[ ] [D]` 移到最前 / 移到最后数据规则核对。
  - Android owner：`DefaultBookListViewModel.moveBooks`
  - 必核规则：只移动非置顶选中项，置顶项保持置顶区
  - 必核写入：最终仍走 `updateBookDataListOrder`
- `[ ] [D]` 置顶 / 取消置顶数据规则核对。
  - Android owner：`BookRepository.pin` / `BookRepository.cancelPin`
  - 必核规则：Book 与 Group 共用全局最大 `pin_order`
  - 必核时间戳：不更新 `updated_date`
- `[ ]` 将 DEBUG 沙盒中的 SwiftUI 网格拖拽验证能力收敛为生产可复用实现。
- `[ ]` 处理置顶区边界：普通项不得拖入置顶区，置顶项不得参与普通排序。
- `[ ]` 拖拽松手后一次性提交 Repository，失败后回滚 UI 顺序。
- `[ ] [V]` 验证 iOS 写入后的 `book_order` / `group_order` / `pinned` / `pin_order` 与 Android DAO 语义一致。

完成标准：
- 普通书和分组跨行拖拽后顺序稳定。
- 移到最前/最后不改变置顶项。
- 置顶追加顺序与 Android 一致。
- iOS 不额外更新时间戳。

### 5. 删除与分组

- `[ ] [D]` 删除书籍级联软删除规则核对。
  - Android owner：`BookRepository.deleteBook`
  - 必核范围：book、tag_book、tag_note、note、attach_image、category、category_image、category_content、review、review_image、chapter、group_book、book_read_status_record、timing、sort setting、check-in、collection_book、read_plan
  - 必核事务：Android 在 `noteDb.runInTransaction` 内执行
  - 必核时间戳：逐 DAO 核对，不能统一假设
- `[ ] [D]` 删除分组 moveOut 规则核对。
  - Android owner：`BookRepository.deleteBookOrGroup` + `GroupRepository.moveOut` + `GroupRepository.deleteGroup`
  - 必核规则：先移出书籍，再删除分组
  - 必核规则：移出书籍取消置顶，软删除 `group_book`
  - 必核规则：按用户选择写入开头或末尾的新 `book_order`
  - 必核时间戳：`deleteGroup` 本身不更新 `updated_date`
- `[ ] [D]` 移入分组事务规则核对。
  - Android owner：`GroupRepository.moveBooksToGroup`
  - 必核事务：`noteDb.runInTransaction`
  - 必核规则：取消置顶、删除旧关系、插入新关系、目标分组内追加排序
  - 必核时间戳：相关 `group_book` / `book_order` 更新需对齐 Android
- `[ ]` 实现 iOS Repository 删除书籍写入。
- `[ ]` 实现 iOS Repository 删除分组写入。
- `[ ]` 实现 iOS Repository 移入分组写入。
- `[ ]` 删除分组弹窗明确询问组内书籍放到开头或末尾。
- `[ ] [V]` 验证关联表、时间戳、软删除状态、重新读取结果。

完成标准：
- 删除书籍不会遗留 Android 会删除的关联数据。
- 删除分组后组内书籍位置与 Android 规则一致。
- 移入分组后 Android 端可按既有读取规则得到一致结果。

### 6. 二级批量管理能力

- `[ ] [D]` 批量标签写入规则核对。
- `[ ] [D]` 批量来源写入规则核对。
- `[ ] [D]` 批量阅读状态写入规则核对。
- `[ ] [D]` 批量加入收藏/书单规则核对。
- `[ ] [D]` 批量导出规则核对，确认是否涉及数据库写入或仅生成文件。
- `[ ]` 将低频批量动作放入二级管理入口，降低首页编辑态负担。
- `[ ] [V]` 对每类批量动作验证最终表状态与同步影响。

完成标准：
- 二级批量动作不阻塞首页高频排序、置顶、删除和分组能力上线。
- 每项动作都有独立 `[D]` 和 `[V]` 记录。

### 7. 收口与治理

- `[ ]` 更新 `docs/feature/首页书架管理/需求文档.md`。
- `[ ]` 更新 `docs/feature/首页书架管理/设计文档.md`。
- `[ ]` 更新 `docs/feature/首页书架管理/对齐情况.md`。
- `[ ]` 在《对齐情况.md》中逐项列出 iOS 行为、Android 行为、对齐状态、双端代码证据。
- `[ ]` 对未对齐项标注“功能优化”或“设计倒退”，并给出 Android 反向优化建议。
- `[ ]` 补充 `docs/learning/` 下的 SwiftUI / Android Compose 对照学习总结。
- `[ ]` 如新增或沉淀 UI 组件，更新 `docs/component-guides/`、术语表、组件清单和白名单。
- `[ ]` 执行构建校验。
- `[ ]` 执行文档治理脚本。

完成标准：
- 收到“任务已完成”信号后一次性补齐治理文档。
- 构建与文档闸门通过。

## Repository 契约草案

正式实现前需基于数据语义核对结果收敛命名和参数，初始建议如下：

```swift
protocol BookRepositoryProtocol {
    func observeBookshelf(setting: BookshelfDisplaySetting, searchKeyword: String?) -> AsyncThrowingStream<[BookshelfItem], Error>
    func updateBookshelfOrder(_ orderedItems: [BookshelfOrderItem]) async throws
    func pinBookshelfItems(_ items: [BookshelfItemID]) async throws
    func unpinBookshelfItem(_ item: BookshelfItemID) async throws
    func moveBookshelfItemsToStart(_ items: [BookshelfItemID]) async throws
    func moveBookshelfItemsToEnd(_ items: [BookshelfItemID]) async throws
    func deleteBookshelfItems(_ items: [BookshelfItemID], groupBooksPlacement: GroupBooksPlacement) async throws
    func moveBooks(_ bookIDs: [Int64], toGroup targetGroupID: Int64) async throws
}
```

约束：
- `BookshelfItemID` 必须区分 Book 与 Group，避免误把 Group ID 当 Book ID 写入。
- Repository 接口只接收业务意图和最终顺序，不暴露 GRDB。
- 所有接口实现前必须把对应 Android 数据语义核对任务标为 `[D]`。

## 测试计划

### Repository 验证

- 验证手动排序后 `book_order` / `group_order` 从 `0` 开始连续写入。
- 验证手动排序不修改 `updated_date` / `last_sync_date`。
- 验证置顶追加使用 Book / Group 全局最大 `pin_order + 1`。
- 验证取消置顶只写 `pinned = 0, pin_order = 0`。
- 验证删除书籍后所有 Android 级联范围内的关联表状态。
- 验证删除分组时组内书籍先移出，再软删除分组。
- 验证移入分组时取消置顶、删除旧关系、插入新关系、目标组追加排序。
- 验证所有写入的 `is_deleted` 条件与 Android DAO 一致。

### UI 手动验证

- 六个维度均能切换并展示只读骨架。
- 默认维度保持 Book / Group 混排顺序。
- 标签、来源、评分、作者聚合数量与代表封面稳定，代表封面不超过 6 张。
- 搜索态能过滤当前维度内容，空结果有明确空态。
- 设置 Sheet 的列数和书摘数量开关只影响 UI，不写数据库。
- 普通书跨行拖拽排序，界面顺序和 order log 一致。
- 置顶项不可被普通排序影响。
- 搜索态禁止排序。
- 切换列数后不丢失当前顺序。
- 删除分组时明确选择组内书籍放到开头或末尾。
- iOS 写入后退出重进，读取顺序稳定。

### 构建与治理

```bash
xcodebuild -project xmnote.xcodeproj -scheme xmnote -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
bash scripts/verify_glossary.sh
bash scripts/verify_ui_glossary_scope.sh
bash scripts/verify_view_component_boundaries.sh
bash scripts/verify_l3_protocol_headers.sh
bash scripts/verify_arch_docs_sync.sh
bash scripts/verify_component_guides.sh
```

治理脚本仅在收口阶段执行；开发阶段默认只执行必要构建校验。

## 风险与决策记录

| 状态 | 风险 | 处理策略 |
| --- | --- | --- |
| `[ ]` | Android 某些写入不更新 `updated_date`，可能影响同步触发判断 | iOS 必须逐项复刻，不自行补时间戳 |
| `[ ]` | 删除书籍级联范围大，容易遗漏关联表 | 先建立 DAO 级删除矩阵，再实现 iOS Repository |
| `[ ]` | Book / Group 混排容易出现 ID 类型混淆 | 引入显式 `BookshelfItemID` 区分实体类型 |
| `[ ]` | SwiftUI 网格排序不是系统现成能力 | 先复用 DEBUG 沙盒验证成果，再迁入生产模块 |
| `[ ]` | 首页操作密度过高会复刻 Android 当前产品负担 | 高频动作首页化，低频动作二级化 |
| `[x]` | Phase 2 骨架可能误引入设置持久化或同步副作用 | 本轮显示设置只保存在 `BookViewModel` 内存态，不写 UserDefaults、数据库或同步字段 |

## 当前未完成项

- 尚未完整核对删除书籍涉及的每个 DAO 更新时间戳规则。
- 尚未确认 iOS 是否存在所有 Android 级联删除目标表的 Record 与迁移。
- 尚未实现生产 Repository 写入接口。
- 尚未将 DEBUG 手动排序沙盒迁入生产书架页。
- 尚未实现聚合卡二级详情列表与作者索引真实滚动定位。
