# Domain/
成员清单
- Models/RepositoryModels.swift: 定义仓储跨层模型（NoteDetailPayload、BackupServerFormInput）。
- Repositories/RepositoryProtocols.swift: 定义 Book/Note/BackupServer/Backup 仓储契约。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
