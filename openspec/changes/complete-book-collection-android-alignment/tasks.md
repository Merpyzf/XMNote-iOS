## 1. 双端事实核对

- [x] 1.1 核对 Android 书单添加链路：`EditCollectionActivity`、`BookSearchSheet`、手动创建结果、在线搜索结果、`isDeleted = 1` 写入条件和多选事件。
- [x] 1.2 核对 Android 占位书恢复链路：书单 Item 状态、点击行为、`BooksOfCollectionPresenter.addToRead`、`BookRepository.addBookToRead` 的字段写入、排序和阅读状态影响。
- [x] 1.3 核对 Android 微信读书书单导入链路：系统分享分发、链接识别、HTML 解析字段、事务保存、推荐语写入和错误处理。
- [x] 1.4 核对 Android 当前书单导出与分享长图链路：入口、导出 bookIds 范围、分享图字段、评分与 relation 文本展示规则。
- [x] 1.5 核对 Android 年度书单简介编辑与书单分组记忆链路：年度标题禁用、简介写入、`getShowBookCollectionType` / `setCurrShowBookCollectionType`。
- [x] 1.6 核对 iOS 当前 owner：`BookCollectionDetailView`、`BookCollectionDetailViewModel`、`BookCollectionVisualComponents`、`BookCollectionSheets`、`BookRepository+Collections`、`BookPickerView`、`BookReadQuery`。
- [x] 1.7 涉及分享、文件导出、SwiftUI 渲染、Liquid Glass 或系统分享 API 前，查 Apple 官方文档或项目内 iOS 26 参考文档，并记录采用边界到实现说明。

## 2. 数据模型与 Repository 底座

- [x] 2.1 扩展书单详情查询模型，使手动书单能读取并展示 `book.is_deleted = 1` 的占位书关系，同时年度书单仍只同步有效读完书籍。
- [x] 2.2 为 `BookCollectionBookItem` 或相关展示模型增加占位状态字段，区分有效书、占位书和可恢复动作。
- [x] 2.3 扩展 Repository 添加书籍接口，使本地有效书、在线候选书、手动创建结果和占位书草稿能统一写入当前手动书单。
- [x] 2.4 实现占位书恢复 Repository 能力，按 Android 对齐规则更新 `book.is_deleted`、书架排序和必要阅读状态，并保留原书单 relation。
- [x] 2.5 设计并实现微信读书导入预览数据结构，包含书单标题、简介、书籍标题、作者、封面和推荐语。
- [x] 2.6 实现微信读书导入保存 Repository 能力，确认后在单个事务中创建书单、书籍记录和 `collection_book` relation。
- [x] 2.7 为当前书单导出与分享长图提供稳定的书单快照查询，包含有效书、占位书、relation 文本和书单本体信息。
- [x] 2.8 为年度书单本体说明提供 Repository 写入口，允许更新年度书单简介但不允许修改标题、年份、成员或排序。
- [x] 2.9 扩展书单显示偏好存储，保存最近选择的书单分组，避免新增业务数据库 schema。

## 3. 完整添加书籍流程

- [x] 3.1 扩展 `BookPickerConfiguration` / `BookPickerScope`，支持书单场景的本地 + 在线组合搜索。
- [x] 3.2 打通 `allowsCreationFlow` 在书单添加场景中的创建书籍流程，并让创建结果返回当前书单详情。
- [x] 3.3 让书单详情底部 Liquid Glass `添加书籍` 继续作为唯一高频入口，点击后打开完整选择流程。
- [x] 3.4 处理选择器中已存在于当前书单的书籍：预选、禁用重复选择或保存时去重，并给出非打断反馈。
- [x] 3.5 将选择器返回的本地、多选、在线、手动创建结果统一交给 ViewModel，再由 ViewModel 调用 Repository 写入书单。
- [x] 3.6 保持年度书单、排序态和写操作进行中状态不显示或不允许触发添加入口。

## 4. 占位书展示与恢复

- [x] 4.1 更新书单详情 Item，让占位书继续显示封面、标题、作者、简介占位和 relation 文本。
- [x] 4.2 为占位书增加低噪音状态表达，使用 `未加入书架`、`加入书架` 或最终确认的 iOS 语义，不照搬 Android 的模糊 `添加`。
- [x] 4.3 为占位书 Item 主体提供打开行为，进入带恢复入口的书籍信息页或占位书预览页。
- [x] 4.4 为占位书 Item 增加恢复入口，点击后执行 Repository 恢复写入并提供即时反馈。
- [x] 4.5 恢复成功后刷新书单详情，确认 relation 文本、排序和封面信息不丢失。
- [x] 4.6 恢复失败时保留占位状态，展示失败反馈，不破坏当前书单列表。
- [x] 4.7 确认手动书单移出占位书只影响当前 relation，不误删其他书单或后续导入可引用的书籍记录。

## 5. 微信读书书单导入

- [x] 5.1 在书单模块增加 `导入微信读书书单` 入口，位置优先放在书单首页或书单模块更多菜单，不放入底部添加浮层。
- [x] 5.2 实现链接输入或粘贴界面，校验微信读书书单链接格式并给出错误反馈。
- [x] 5.3 接入系统分享分发入口，将用户分享给 XMNote 的微信读书书单链接路由到同一导入预览流程。
- [x] 5.4 通过 Repository 解析微信读书书单链接，成功后展示导入预览 Sheet。
- [x] 5.5 导入预览展示书单标题、简介、书籍数量、书籍标题、作者和推荐语，不在预览前写入数据库。
- [x] 5.6 用户确认导入后，通过 Repository 事务保存书单、占位书关系和 relation 推荐语。
- [x] 5.7 导入成功后进入新书单详情或刷新书单首页，并给出成功反馈。
- [x] 5.8 导入失败、用户取消或网络异常时不写入半成品数据，并展示明确反馈。

## 6. 当前书单真实导出

- [x] 6.1 将 `BookCollectionDetailViewModel.presentCollectionExportPlaceholder` 替换为真实当前书单导出触发逻辑。
- [x] 6.2 设计当前书单导出上下文，锁定书单 ID、标题、类型和 relation 字段，避免退化为全书架导出。
- [x] 6.3 接入现有书籍导出模块或新增书单导出适配层，默认导出当前书单内书籍基础信息。
- [x] 6.4 导出字段包含普通书单 `收藏理由`、年度书单 `年度点评`，并对占位书标记未加入书架状态。
- [x] 6.5 空书单点击导出时显示无法导出的反馈，不生成误导性空成功文件。
- [x] 6.6 导出成功后提供系统分享、保存或查看结果的后续动作；导出失败时保留详情页上下文。
- [x] 6.7 确认写操作进行中和排序态会禁用或阻止导出入口。

## 7. 书单分享长图

- [x] 7.1 在书单详情更多菜单增加 `分享书单` 入口，手动书单和年度书单均可见。
- [x] 7.2 设计分享图快照模型，与结构化导出共用只读书单快照但独立输出模板。
- [x] 7.3 实现分享图模板，包含书单标题、简介、数量、封面、书名、作者、评分或阅读信息、收藏理由/年度点评。
- [x] 7.4 对无 relation 文本的书籍省略 relation 区，不显示空标题或空块。
- [x] 7.5 对长书单设置稳定渲染策略：分页、分段长图或前 N 本展示，并明确剩余数量。
- [x] 7.6 生成分享图期间展示生成中反馈并禁用重复触发。
- [x] 7.7 生成成功后打开系统分享或保存图片动作；失败时展示失败反馈并保留当前页面。

## 8. 年度说明与分组记忆

- [x] 8.1 在年度书单详情更多菜单增加 `编辑年度说明` 或最终确认文案的入口。
- [x] 8.2 新增年度说明编辑 Sheet，仅编辑年度本体说明，不提供标题、年份、成员或排序编辑。
- [x] 8.3 在年度书单 Header 或简介区展示年度说明，空文本时保持界面简洁。
- [x] 8.4 保存年度说明时通过 Repository 更新年度书单简介字段，并展示保存反馈。
- [x] 8.5 确认年度说明与单本书 `年度点评` 在文案、位置、VoiceOver 语义上不混淆。
- [x] 8.6 在书单首页切换 `我的书单` / `年度书单` 时保存最近选择分组。
- [x] 8.7 进入书单首页时恢复最近选择分组；当年度书单为空且我的书单非空时回退到我的书单。
- [x] 8.8 保持分组偏好为 UI 偏好，不写入书单业务表，也不影响同步。

## 9. UI、可访问性与状态边界

- [x] 9.1 保持顶部返回与更多按钮、底部 `添加书籍` 浮层的 iOS 26 Liquid Glass 边界，内容区不新增装饰性玻璃。
- [x] 9.2 所有新增生产文本使用 `AppTypography` 和语义色，书籍封面继续使用 `XMBookCover`。
- [x] 9.3 为添加、恢复、导入、导出、分享、年度说明保存提供写操作即时反馈和禁用重复触发。
- [x] 9.4 检查深色模式、动态字体、Reduce Motion、VoiceOver 下新增入口、Sheet、Item 状态和分享/导出反馈可读可操作。
- [x] 9.5 确认新增 Sheet 归位到 `xmnote/Views/Book/Sheets/`，页面私有组件归位到 `xmnote/Views/Book/Components/`，跨模块复用成立时才迁入 `UIComponents`。

## 10. 构建与手动验收

- [x] 10.1 运行构建校验：`xcodebuild -project xmnote.xcodeproj -scheme xmnote -destination 'platform=iOS Simulator,name=iPhone 17 Pro iOS 26.2 Clean' build`。
- [x] 10.2 手动检查手动书单：本地书、在线书、手动创建书、多选结果均可加入当前书单。
- [x] 10.3 手动检查占位书：展示、打开、加入书架、恢复失败、移出关系都符合预期。
- [x] 10.4 手动检查微信读书导入：有效链接预览并保存，无效链接、取消、网络失败均不写入半成品。
- [x] 10.5 手动检查当前书单导出：范围锁定当前书单，空书单、占位书、收藏理由、年度点评均按规则处理。
- [x] 10.6 手动检查书单分享：普通书单、年度书单、长书单、无 relation 文本和生成失败场景均可用。
- [x] 10.7 手动检查年度说明：编辑、清空、展示、与年度点评区分、年度成员自动同步均符合预期。
- [x] 10.8 手动检查书单分组记忆：重进页面恢复最近分组，年度空态回退符合规则。
