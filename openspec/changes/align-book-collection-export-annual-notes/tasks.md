## 1. 导出入口占位

- [x] 1.1 在 `BookCollectionDetailViewModel` 增加书单导出占位方法，点击后只设置现有顶部反馈 banner，不触发路由、系统分享、文件选择器或真实导出任务
- [x] 1.2 调整 `BookCollectionDetailView` 的更多菜单显示条件，使手动书单和年度书单详情加载完成后都能打开更多菜单
- [x] 1.3 在更多菜单顶部新增 `导出此书单` 操作区，手动书单和年度书单都显示，并在写操作或排序态下禁用或阻止重复触发
- [x] 1.4 确认底部 Liquid Glass `添加书籍` 浮层、书籍 Item context menu、书籍 Item swipe action 不新增整本书单导出入口

## 2. 年度点评语义与权限

- [x] 2.1 为书单 relation 文本增加页面级展示语义配置，按书单类型输出 `收藏理由` 或 `年度点评` 相关标题、入口、占位、保存中、保存成功和可访问性文案
- [x] 2.2 将书籍 Item 的编辑权限拆分为结构编辑权限和 relation 文本编辑权限，年度书单只允许编辑 relation 文本，不允许添加、移出、排序或编辑书单本体
- [x] 2.3 更新 `BookCollectionBookCard`、备注展示区和空态入口，使普通书单继续显示 `收藏理由`，年度书单显示 `年度点评`
- [x] 2.4 更新备注编辑 Sheet，使标题、subtitle、placeholder、底部说明和保存反馈根据普通书单/年度书单语义切换
- [x] 2.5 将年度书单内容标题区状态从 `只读` 调整为 `自动同步`，表达成员来自读完记录但年度点评可编辑

## 3. 数据写入与边界核查

- [x] 3.1 复用现有 `updateCollectionBookRecommend` Repository 写入口保存年度点评，不新增数据库字段、迁移或直接数据库访问
- [x] 3.2 确认新增、编辑、清空年度点评时不会改变年度书单成员、排序、读完年份或书籍阅读状态
- [x] 3.3 确认手动书单的添加书籍、编辑书单信息、调整排序、删除书单、移出书籍和收藏理由行为保持不变

## 4. 验证

- [x] 4.1 运行构建校验：`xcodebuild -project xmnote.xcodeproj -scheme xmnote -destination 'platform=iOS Simulator,name=iPhone 17 Pro iOS 26.2 Clean' build`
- [x] 4.2 手动检查手动书单详情：更多菜单显示 `导出此书单`，点击仅显示占位反馈；底部仍只显示 `添加书籍`
- [x] 4.3 手动检查年度书单详情：更多菜单显示 `导出此书单`，不显示结构编辑操作；Item 可添加、编辑、清空 `年度点评`
- [x] 4.4 手动检查普通书单 Item：`收藏理由` 文案、编辑入口、保存反馈和 VoiceOver 语义不回退或串到年度点评
- [x] 4.5 手动检查深色模式、动态字体、VoiceOver 下菜单入口、点评入口、编辑 Sheet 和反馈 banner 可读可操作
